import { registerRootComponent } from 'expo';
import React from 'react';
import { Text } from 'react-native';

function App() {
  return <Text>OMS Kotlin SDK Expo compatibility</Text>;
}

registerRootComponent(App);
